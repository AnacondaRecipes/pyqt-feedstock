{% set version = "5.11.3" %}
{% set sip_version = "4.19.13" %}

package:
  name: pyqt
  version: {{ version }}

source:
  - url: http://sourceforge.net/projects/pyqt/files/PyQt5/PyQt-{{ version }}/PyQt5_gpl-{{ version }}.tar.gz
    # 5.11.3
    sha1: 497c25e36ad982034d2a18075f34a67eb113fba8
    # 5.11.2
    # sha1: c865073fb614520b18c4f44bc4cbea711a0ab2a4 
    patches:
      - 0001-Ignore-lack-of-shared-python-lib.patch
  - url: http://sourceforge.net/projects/pyqt/files/sip/sip-{{ sip_version }}/sip-{{ sip_version }}.tar.gz
    sha256: e353a7056599bf5fbd5d3ff9842a6ab2ea3cf4e0304a0f925ec5862907c0d15e
    folder: sip
    patches:
      - 0001-Do-not-override-CC-CXX-LINK-in-sipgen-Makefile.patch

build:
  number: 0
  skip: true  # [win and py27]
  skip: true  # [ppc64le]
  run_exports:
    {{ pin_subpackage('pyqt', max_pin='x.x') }}

requirements:
  build:
    # We may want to find and add a Python dbus library?
    # pkg-config --cflags-only-I --libs dbus-1
    # The Python dbus module doesn't seem to be installed.
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - {{ cdt('xorg-x11-proto-devel') }}  # [linux]
    - {{ cdt('libx11-devel') }}          # [linux]
    - {{ cdt('libxcb') }}                # [linux]
    - {{ cdt('libxext-devel') }}         # [linux]
    - {{ cdt('libxrender-devel') }}      # [linux]
    - {{ cdt('mesa-libgl-devel') }}      # [linux]
    - {{ cdt('mesa-libegl-devel') }}     # [linux]
    - {{ cdt('mesa-dri-drivers') }}      # [linux]
    - {{ cdt('libxau-devel') }}          # [linux]
    - {{ cdt('alsa-lib-devel') }}        # [linux]
    - {{ cdt('gtk2-devel') }}            # [linux]
    - {{ cdt('gtkmm24-devel') }}         # [linux]
    - {{ cdt('libdrm-devel') }}          # [linux]
    - {{ cdt('libxcomposite-devel') }}   # [linux]
    - {{ cdt('libxcursor-devel') }}      # [linux]
    - {{ cdt('libxi-devel') }}           # [linux]
    - {{ cdt('libxrandr-devel') }}       # [linux]
    - {{ cdt('pciutils-devel') }}        # [linux]
    - {{ cdt('libxscrnsaver-devel') }}   # [linux]
    - {{ cdt('libxtst-devel') }}         # [linux]
    - {{ cdt('libselinux-devel') }}      # [linux]
    - {{ cdt('libxdamage') }}            # [linux]
    - {{ cdt('libxfixes') }}             # [linux]
    - {{ cdt('libxxf86vm') }}            # [linux]
    - {{ cdt('mesa-libgbm') }}           # [linux]
    - jom                                # [win]
    - pkg-config                         # [not win]
  host:
    - python
    - enum34                             # [py==27]
    - dbus                               # [linux]]
    - qt 5.12.*
  run:
    - python
    - enum34                             # [py==27]
    # Not sure if this is necessary?
    # (it could even be harmful, see comment in build.sh)
    - sip >=4.19.13,<4.20a0

test:
  files:
    - pyqt_test.py
  imports:
    - PyQt5.sip
    - PyQt5.QtCore
    - PyQt5.QtGui
    - PyQt5.QtWebEngineWidgets
  commands:
    - DISPLAY=localhost:1.0 xvfb-run -a bash -c 'python pyqt_test.py'  # [linux]

about:
  home: http://www.riverbankcomputing.co.uk/software/pyqt
  license: Commercial, GPL-2.0, GPL-3.0
  license_family: GPL
  license_file: LICENSE
  summary: 'Python binding of the cross-platform GUI toolkit Qt.'
  description: |
    "PyQt is a set of Python v2 and v3 bindings for The Qt Company's Qt
    application framework and runs on all platforms supported by Qt including
    Windows, MacOS/X and Linux. PyQt5 supports Qt v5. PyQt4 supports Qt v4 and
    will build against Qt v5. The bindings are implemented as a set of Python
    modules and contain over 1,000 classes."
  doc_url: https://www.riverbankcomputing.com/software/pyqt/
  dev_url: https://github.com/pyqt

extra:
  recipe-maintainers:
    - ccordoba12
    - mingwandroid
    - gillins
    - msarahan
    - ocefpaf
